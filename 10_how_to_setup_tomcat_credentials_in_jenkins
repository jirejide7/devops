http://$JENKINS_URL:$JENKINS_PORT
Jenkins
- Credentials
  - Global
    - Add Credentials
      - Username > jenkins
      - Password > $TOMCAT_MANAGER_SCRIPT_PASSWORD
      - ID > tomcat_key
      - Description > Tomcat Key
    - OK
