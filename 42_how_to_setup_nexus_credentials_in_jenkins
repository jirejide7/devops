# In browser
http://$UBUNTU_EC2_PUBLIC_IP:$JENKINS_PORT
login
- Username > $JENKINS_USERNAME
- Password > $JENKINS_PASSWORD

Jenkins
- Credentials
  - global
    - Add credentials
      - Username > admin
      - Password > $NEXUS_PASSWORD
      - ID > nexus_key
      - Description > Nexus Key
OK
