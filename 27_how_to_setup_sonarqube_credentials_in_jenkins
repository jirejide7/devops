# In browser
http://$EC2_INSTANCE_IP:$JENKINS_PORT
http://$JENKINS_URL:$JENKINS_PORT
login
- Username > $JENKINS_USERNAME
- Password > $JENKINS_PASSWORD

Jenkins
- Credentials
  - global
    - Add Credentials
      - Kind > Secret text
      - Secret > paste sonarqube token
      - ID > sonarqube_key
      - Description > SonarQube Key
    OK
