sudo su
yum install -y wget
NEXUS=nexus-3.19.0-01
ls
wget http://download.sonatype.com/nexus/3/$NEXUS-unix.tar.gz
ls
tar -xvf $NEXUS-unix.tar.gz --directory /opt
ls /opt
mv /opt/$NEXUS /opt/nexus
ls /opt
exit
