git checkout -b java
nano Jenkinsfile
node('docker') {
    checkout scm
    stage('Build') {
        docker.image('maven:3.3.3').inside {
            sh 'mvn --version'
        }
    }
}
CTRL+X
y
git status
git add Jenkinsfile
git commit -m "added Jenkinsfile for Java" 
git push github java
